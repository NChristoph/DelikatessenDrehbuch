
@model List<IngredientHandlerModel>


@{

    Model.RemoveAll(i => i.Ingredient.Name == "Salz" || i.Ingredient.Name == "Pfeffer");
    var sortedList = Model.OrderBy(x => x.Quantity.Quantitys).Reverse();
    foreach (var ingredient in sortedList)
    {
        <div class="row p-1 mobieleWidth100 mt-1 minWidth100">
            <div class="col minWidth30"
                 style="white-space:normal;text-align:start">
                @ingredient.Ingredient.Name
            </div>
            <div class="col minWidth20 maxWidth20">
                @ingredient.Quantity.Quantitys
            </div>
            <div class="col minWidth10 maxWidth10">
                @ingredient.Measure.UnitOfMeasurement
            </div>

        </div>
    }
    <span class="p-1">Salz und Pfeffer</span>
}